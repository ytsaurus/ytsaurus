# Generated by devtools/yamaker.

LIBRARY()

LICENSE(
    Apache-2.0 AND
    BSL-1.0 AND
    CC0-1.0 AND
    MIT
)

LICENSE_TEXTS(.yandex_meta/licenses.list.txt)

VERSION(24.8.14.39)

PEERDIR(
    contrib/clickhouse/base/poco/Net
)

ADDINCL(
    GLOBAL contrib/clickhouse/base/poco/Redis/include
    contrib/clickhouse/base/poco/Foundation/include
    contrib/clickhouse/base/poco/Net/include
)

NO_COMPILER_WARNINGS()

NO_UTIL()

IF (OS_DARWIN)
    CFLAGS(
        GLOBAL -DOS_DARWIN
    )
ELSEIF (OS_LINUX)
    CFLAGS(
        GLOBAL -DOS_LINUX
    )
ENDIF()

IF (OS_LINUX)
    CFLAGS(
        -DPOCO_HAVE_FD_EPOLL
    )
ENDIF()

CFLAGS(
    -DPOCO_ENABLE_CPP11
    -DPOCO_OS_FAMILY_UNIX
    -D_LIBCPP_ENABLE_THREAD_SAFETY_ANNOTATIONS
    -D_LIBUNWIND_IS_NATIVE_ONLY
)

SRCS(
    src/Array.cpp
    src/AsyncReader.cpp
    src/Client.cpp
    src/Command.cpp
    src/Error.cpp
    src/Exception.cpp
    src/RedisEventArgs.cpp
    src/RedisStream.cpp
    src/Type.cpp
)

END()
