# THIS FILE IS GENERATED. Do not edit manually.
# Regenerate: python .github/workflowsgen/gen_all.py

name: Go SDK CI
on:
  push:
    branches:
    - main
jobs:
  check-yt-go:
    name: Run checks (yt/go)
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Prepare Dockerfile
      shell: bash
      run: |
        set -x
        echo "FROM ubuntu:20.04 " > Dockerfile
        echo "WORKDIR /home/ytsaurus " >> Dockerfile
        echo "RUN apt update && apt install -y ca-certificates wget build-essential curl" >> Dockerfile
        echo "RUN wget https://go.dev/dl/go1.23.8.linux-amd64.tar.gz && tar -C /usr/local -xzf go1.23.8.linux-amd64.tar.gz" >> Dockerfile
        echo "COPY . /home/ytsaurus/ " >> Dockerfile
        echo 'ENTRYPOINT ["tail", "-f", "/dev/null"]' >> Dockerfile
    - name: Build Docker image
      shell: bash
      run: |
        set -x
        docker build -t test-image .
    - name: Run Docker container
      shell: bash
      working-directory: yt/go
      run: |
        set -x
        docker run --privileged -v "/var/run/docker.sock":"/var/run/docker.sock" --network host -d --name test-container test-image
    - name: Build
      shell: bash
      working-directory: yt/go
      run: |
        set -x
        docker exec test-container /bin/bash -c "export PATH=\$PATH:/usr/local/go/bin && cd yt/go && go build -v -mod=readonly ./..."
    - name: Run tests
      shell: bash
      working-directory: yt/go
      run: |
        set -x
        docker exec test-container /bin/bash -c "export PATH=\$PATH:/usr/local/go/bin && cd yt/go && go test -v -mod=readonly ./..."
    - name: Run tests with race detector
      shell: bash
      working-directory: yt/go
      run: |
        set -x
        docker exec test-container /bin/bash -c "export PATH=\$PATH:/usr/local/go/bin && cd yt/go && go test -v -mod=readonly -race ./..."
  check-library-go-core-log:
    name: Run checks (library/go/core/log)
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Prepare Dockerfile
      shell: bash
      run: |
        set -x
        echo "FROM ubuntu:20.04 " > Dockerfile
        echo "WORKDIR /home/ytsaurus " >> Dockerfile
        echo "RUN apt update && apt install -y ca-certificates wget build-essential curl" >> Dockerfile
        echo "RUN wget https://go.dev/dl/go1.23.8.linux-amd64.tar.gz && tar -C /usr/local -xzf go1.23.8.linux-amd64.tar.gz" >> Dockerfile
        echo "COPY . /home/ytsaurus/ " >> Dockerfile
        echo 'ENTRYPOINT ["tail", "-f", "/dev/null"]' >> Dockerfile
    - name: Build Docker image
      shell: bash
      run: |
        set -x
        docker build -t test-image .
    - name: Run Docker container
      shell: bash
      working-directory: library/go/core/log
      run: |
        set -x
        docker run --privileged -v "/var/run/docker.sock":"/var/run/docker.sock" --network host -d --name test-container test-image
    - name: Build
      shell: bash
      working-directory: library/go/core/log
      run: |
        set -x
        docker exec test-container /bin/bash -c "export PATH=\$PATH:/usr/local/go/bin && cd library/go/core/log && go build -v -mod=readonly ./..."
    - name: Run tests
      shell: bash
      working-directory: library/go/core/log
      run: |
        set -x
        docker exec test-container /bin/bash -c "export PATH=\$PATH:/usr/local/go/bin && cd library/go/core/log && go test -v -mod=readonly ./..."
    - name: Run tests with race detector
      shell: bash
      working-directory: library/go/core/log
      run: |
        set -x
        docker exec test-container /bin/bash -c "export PATH=\$PATH:/usr/local/go/bin && cd library/go/core/log && go test -v -mod=readonly -race ./..."
  check-library-go-x-xreflect:
    name: Run checks (library/go/x/xreflect)
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Prepare Dockerfile
      shell: bash
      run: |
        set -x
        echo "FROM ubuntu:20.04 " > Dockerfile
        echo "WORKDIR /home/ytsaurus " >> Dockerfile
        echo "RUN apt update && apt install -y ca-certificates wget build-essential curl" >> Dockerfile
        echo "RUN wget https://go.dev/dl/go1.23.8.linux-amd64.tar.gz && tar -C /usr/local -xzf go1.23.8.linux-amd64.tar.gz" >> Dockerfile
        echo "COPY . /home/ytsaurus/ " >> Dockerfile
        echo 'ENTRYPOINT ["tail", "-f", "/dev/null"]' >> Dockerfile
    - name: Build Docker image
      shell: bash
      run: |
        set -x
        docker build -t test-image .
    - name: Run Docker container
      shell: bash
      working-directory: library/go/x/xreflect
      run: |
        set -x
        docker run --privileged -v "/var/run/docker.sock":"/var/run/docker.sock" --network host -d --name test-container test-image
    - name: Build
      shell: bash
      working-directory: library/go/x/xreflect
      run: |
        set -x
        docker exec test-container /bin/bash -c "export PATH=\$PATH:/usr/local/go/bin && cd library/go/x/xreflect && go build -v -mod=readonly ./..."
    - name: Run tests
      shell: bash
      working-directory: library/go/x/xreflect
      run: |
        set -x
        docker exec test-container /bin/bash -c "export PATH=\$PATH:/usr/local/go/bin && cd library/go/x/xreflect && go test -v -mod=readonly ./..."
    - name: Run tests with race detector
      shell: bash
      working-directory: library/go/x/xreflect
      run: |
        set -x
        docker exec test-container /bin/bash -c "export PATH=\$PATH:/usr/local/go/bin && cd library/go/x/xreflect && go test -v -mod=readonly -race ./..."
  check-library-go-blockcodecs:
    name: Run checks (library/go/blockcodecs)
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Prepare Dockerfile
      shell: bash
      run: |
        set -x
        echo "FROM ubuntu:20.04 " > Dockerfile
        echo "WORKDIR /home/ytsaurus " >> Dockerfile
        echo "RUN apt update && apt install -y ca-certificates wget build-essential curl" >> Dockerfile
        echo "RUN wget https://go.dev/dl/go1.23.8.linux-amd64.tar.gz && tar -C /usr/local -xzf go1.23.8.linux-amd64.tar.gz" >> Dockerfile
        echo "COPY . /home/ytsaurus/ " >> Dockerfile
        echo 'ENTRYPOINT ["tail", "-f", "/dev/null"]' >> Dockerfile
    - name: Build Docker image
      shell: bash
      run: |
        set -x
        docker build -t test-image .
    - name: Run Docker container
      shell: bash
      working-directory: library/go/blockcodecs
      run: |
        set -x
        docker run --privileged -v "/var/run/docker.sock":"/var/run/docker.sock" --network host -d --name test-container test-image
    - name: Build
      shell: bash
      working-directory: library/go/blockcodecs
      run: |
        set -x
        docker exec test-container /bin/bash -c "export PATH=\$PATH:/usr/local/go/bin && cd library/go/blockcodecs && go build -v -mod=readonly ./..."
    - name: Run tests
      shell: bash
      working-directory: library/go/blockcodecs
      run: |
        set -x
        docker exec test-container /bin/bash -c "export PATH=\$PATH:/usr/local/go/bin && cd library/go/blockcodecs && go test -v -mod=readonly ./..."
    - name: Run tests with race detector
      shell: bash
      working-directory: library/go/blockcodecs
      run: |
        set -x
        docker exec test-container /bin/bash -c "export PATH=\$PATH:/usr/local/go/bin && cd library/go/blockcodecs && go test -v -mod=readonly -race ./..."
  send-alert:
    name: Check status and send alert
    needs:
    - check-yt-go
    - check-library-go-core-log
    - check-library-go-x-xreflect
    - check-library-go-blockcodecs
    if: always()
    runs-on: ubuntu-latest
    steps:
    - name: Checkout notification script
      uses: actions/checkout@v4
      with:
        path: ytsaurus
        sparse-checkout-cone-mode: false
        sparse-checkout: .github/scripts/send_notifications.py
    - name: Prepare Python
      run: |
        set -x
        pip install PyGithub==2.6.1
    - name: Save conclusion (failure)
      if: needs.check-yt-go.result == 'failure' || needs.check-library-go-core-log.result == 'failure' || needs.check-library-go-x-xreflect.result == 'failure' || needs.check-library-go-blockcodecs.result == 'failure'
      run: |
        set -x
        CURRENT_JOB_CONCLUSION=failure
        echo "CURRENT_JOB_CONCLUSION=$CURRENT_JOB_CONCLUSION" >> $GITHUB_ENV
    - name: Save conclusion (success)
      if: "!(needs.check-yt-go.result == 'failure' || needs.check-library-go-core-log.result == 'failure' || needs.check-library-go-x-xreflect.result == 'failure' || needs.check-library-go-blockcodecs.result == 'failure')"
      run: |
        set -x
        CURRENT_JOB_CONCLUSION=success
        echo "CURRENT_JOB_CONCLUSION=$CURRENT_JOB_CONCLUSION" >> $GITHUB_ENV
    - name: Send telegram status report
      id: send-telegram-status-report
      run: |
        set -x
        python3 ytsaurus/.github/scripts/send_notifications.py \
                --repo ${{ github.repository }} \
                --git-token "${{ secrets.GH_PERSONAL_ACCESS_TOKEN }}" \
                --workflow "${{ github.workflow }}" \
                --ref "${{ github.ref_name }}" \
                --tg-token "${{ secrets.TELEGRAM_TOKEN }}" \
                --tg-chat-id ${{ secrets.TELEGRAM_CHAT_ID }} \
                --current-job-conclusion $CURRENT_JOB_CONCLUSION \
                --current-job-id ${{ github.run_id }} \
                --git-server-url "${{ github.api_url }}"
