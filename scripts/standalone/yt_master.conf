{
  rpc_port = 9000;
  monitoring_port = 10000;

  master = {
    cell_id = "deadbabe-faceb00c-deadbabe-faceb00c";
    cell_tag = 9999;
    addresses = ["%%FQDN%%:9000"];
  };

  timestamp_provider = {
    addresses = ["%%FQDN%%:9000"];
  };

  chunk_manager = {
    safe_online_node_count = 1;
    safe_lost_chunk_fraction = 0.01;
  };

  snapshots = {
    path = "/var/lib/yt/master_snapshots";
  };

  changelogs = {
    path = "/var/lib/yt/master_changelogs";
  };

  logging = {
    flush_period = 1000;
    watch_period = 1000;

    rules = [
      {
        min_level = info;
        writers = [ info ];
        categories = [ "*" ];
      }
    ];

    writers = {
      info = { file_name = "/var/log/yt/master.log"; type = file; };
    };
  };
}
