{
  driver = {
    master = {
      addresses = ["%%FQDN%%:9000"];
    };

    master_cache = {
      addresses = ["%%FQDN%%:9000"];
    };

    timestamp_provider = {
      addresses = ["%%FQDN%%:9000"];
    };
  };

  logging = {
    rules = [];
    writers = {};
  };
}
