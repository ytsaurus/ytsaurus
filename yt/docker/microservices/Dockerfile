ARG ROOT="/ytsaurus"

ARG YT_MICROSERVICES_ROOT="${ROOT}/microservices"

FROM ubuntu:24.04

ARG ROOT
ENV ROOT=$ROOT

ARG YT_MICROSERVICES_ROOT
ENV YT_MICROSERVICES_ROOT=$YT_MICROSERVICES_ROOT

COPY . ${YT_MICROSERVICES_ROOT}

WORKDIR ${YT_MICROSERVICES_ROOT}
