## NYT::NFlow::TVanillaTaskConfig {#NYT_NFlow_TVanillaTaskConfig}
<!-- This file is generated by yt/yt/flow/tools/generate_yson_struct_doc/generate.sh script -->
<!-- Before using doc generation tool check readme: yt/yt/flow/tools/generate_yson_struct_doc/README.md -->
Источник: [yt/yt/flow/library/cpp/runner/vanilla_launcher.h]({{source-root}}/yt/yt/flow/library/cpp/runner/vanilla_launcher.h)

#|
|| **Параметр** | **Описание** ||
|| `count` | **Тип**: `int`
**Обязательный параметр**
Число джоб задачи (контроллера или воркера) в vanilla-операции. ||
|| `memory_limit` | **Тип**: `std::optional<`[NYT::NYTree::TSize](./all_yson_structs#NYT_NYTree_TSize)`>`
Лимит памяти на одну джобу. Если не задан, используется общий дефолт — `18 GiB`, одинаковый у контроллера и воркера. ||
|| `cpu_limit` | **Тип**: `std::optional<int>`
Лимит CPU на одну джобу. Если не задан, используется общий дефолт — `6`, одинаковый у контроллера и воркера. ||
|| `port_count` | **Тип**: `std::optional<int>`
Сколько портов запросить у YT (порты выдаются через `YT_PORT_<i>` и перекрывают фиксированные порты из node-config): `YT_PORT_0` — `rpc_port` (и `bus_server.port`), `YT_PORT_1` — `monitoring_port`, `YT_PORT_2` — `companion.port`. Нужно на хосте с общей сетью, где фиксированные порты соседних джоб столкнулись бы: контроллеру и воркеру без компаньона — `2`, воркеру с компаньоном (Python, Java, Go, C++-компаньон) — `3`. При запуске без сетевого проекта поле заполняется автоматически (`2` у контроллера, `3` у воркера); явное значение `0` оставляет фиксированные порты. Как только `port_count` воркера положителен, фиксированный `companion.port = 10082` в конфиг не подставляется: воркер с компаньоном и меньшим числом портов не стартует, вместо того чтобы ходить на компаньона соседней джобы. ||
|| `local_files` | **Тип**: `THashMap<std::string, std::string>`
**Значение по умолчанию**: `{}`
 ||
|| `cypress_files` | **Тип**: `THashMap<std::string, std::string>`
**Значение по умолчанию**: `{}`
 ||
|| `layers` | **Тип**: `std::vector<std::string>`
**Значение по умолчанию**: `[]`
Cypress-пути porto-слоёв, монтируемых в корневую файловую систему задачи. Непустой список хотя бы у одной задачи включает porto-джобы для всей vanilla-операции. ||
|| `system_layer_path` | **Тип**: `std::optional<std::string>`
Базовый OS-слой задачи; переопределяет системный слой по умолчанию. ||
|| `docker_image` | **Тип**: `std::optional<std::string>`
Docker-образ корневой файловой системы задачи — для кластеров, где окружение джобы задаётся образом, а не porto-слоями. Взаимоисключим с `layers`. ||
|#

