#pragma once

#include <yt/yt/core/misc/public.h>

namespace NYT::NLeaseServer {

////////////////////////////////////////////////////////////////////////////////

using TLeaseId = TGuid;

////////////////////////////////////////////////////////////////////////////////

DEFINE_ENUM(ELeaseState,
    ((Unknown)            (0))
    ((Active)             (1))
    ((Revoking)           (2))
);

////////////////////////////////////////////////////////////////////////////////

struct ILease;

DECLARE_REFCOUNTED_STRUCT(ILeaseGuard)
DECLARE_REFCOUNTED_STRUCT(ILeaseManager)

DECLARE_REFCOUNTED_CLASS(TLeaseManagerConfig)

class TLoadContext;
class TSaveContext;

////////////////////////////////////////////////////////////////////////////////

} // namespace NYT::NLeaseServer
