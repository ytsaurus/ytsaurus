# When this file lies inside ${ARCADIA_ROOT}/yt it describes repository environment
# and allows to build YT using same ya.make files from arcadia / git repo.

# This is environment for git repo.
SET(YT_INSIDE_ARCADIA no)

SET(YT_ROOT ".")
SET(YT_PYTHON_ROOT ".")
