{
    "cluster_url" = "%YT_CLUSTER%";

    "proxy_role" = #;

    "path" = "%YT_PIPELINE_PATH%";

    "spec" = {
        "computations" = {
            "noop" = {
                "computation_class_name" = "NYT::NFlow::TProcessFunctionSourceComputation";

                "processing_function" = "NYT::NFlow::NExample::TNoopFunction";

                "source_streams" = {
                    "random" = {
                        "source_class_name" = "NYT::NFlow::TRandomSource";
                    };
                };
            };
        };
    };
}
