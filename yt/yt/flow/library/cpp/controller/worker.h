#pragma once

#include "public.h"

#include <yt/yt/flow/library/cpp/common/worker/public.h>
#include <yt/yt/flow/library/cpp/misc/node_info.h>

#include <yt/yt/client/api/public.h>
#include <yt/yt/core/concurrency/public.h>
#include <yt/yt/core/concurrency/thread_affinity.h>
#include <yt/yt/core/rpc/public.h>

#include <library/cpp/yt/misc/enum.h>
#include <library/cpp/yt/misc/property.h>
#include <yt/yt/core/misc/error.h>

namespace NYT::NFlow::NController {

//! Controller-side representation of a worker.
struct TWorkerInfo
    : public TNodeInfoBase
{
    //! Unique ID generated by controller when the worker is registered.
    NWorker::TIncarnationId ConnectionIncarnationId;
    //! Worker groups provided by worker.
    std::vector<TWorkerGroupId> Groups;
    //! A map of capability->value pairs that the worker is able to provide (provided by worker).
    //! Note that zero value and absence of the key are cases with different meaning.
    //! A requirement with some key and zero value meet the first case while does not meet the second.
    THashMap<std::string, ssize_t> Capabilities;
    //! Instant when the worker was registered.
    TInstant RegisterTime;
    //! Current worker state.
    EWorkerState State{};
};

class TWorker
    : public TRefCounted
{
public:
    TWorker(
        const TNodeInfoBase& nodeInfo,
        NWorker::TIncarnationId connectionIncarnationId,
        std::vector<TWorkerGroupId> groups,
        THashMap<std::string, ssize_t> capabilities);

    DEFINE_BYVAL_RW_PROPERTY(NConcurrency::TLease, Lease);
    DEFINE_BYVAL_RW_PROPERTY(TInstant, LastHeartbeatTime);
    DEFINE_BYVAL_RW_PROPERTY(ui64, HeartbeatSeqNo, 0);

public:
    EWorkerState GetState() const;
    void SetState(EWorkerState state);

    const TWorkerInfo& GetInfo() const;

private:
    TWorkerInfo Info_;
};

DEFINE_REFCOUNTED_TYPE(TWorker);

////////////////////////////////////////////////////////////////////////////////

} // namespace NYT::NFlow::NController
