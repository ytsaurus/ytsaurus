"""Opensource stand-in for the ``yt.yt_sync.core.constants`` module.

Compiled under that module name via ``PY_SRCS(NAMESPACE ...)`` only when
real yt_sync is absent from the build (see ya.make).
"""

from yt.yt.flow.library.python.yt_sync_mini import QUEUE_META_COLUMNS  # noqa: F401
