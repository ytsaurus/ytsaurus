[pytest]
# NB: dynamic_tables uses xdelta encoding that is not supported as python library in opensource, should be fixed in YT-19261.
addopts = --ignore compat --ignore dynamic_tables --strict-markers
markers =
    opensource: mark test to be executed in opensource pipeline
