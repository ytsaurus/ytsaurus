package ru.yandex.spark.yt.wrapper.cypress

object YtAttributes {
  val rowCount = "row_count"
  val optimizeFor = "optimize_for"
  val chunkCount = "chunk_count"
  val `type` = "type"
  val sortedBy = "sorted_by"
  val compressedDataSize = "compressed_data_size"
  val dynamic = "dynamic"
}
