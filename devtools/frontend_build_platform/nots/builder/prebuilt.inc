SET_RESOURCE_URI_FROM_JSON(
    SANDBOX_RESOURCE_URI
    resources.json
)

ENABLE(PREBUILT)

PREBUILT_PROGRAM()

IF (SANDBOX_RESOURCE_URI)
    DECLARE_EXTERNAL_RESOURCE(NOTS_BUILDER ${SANDBOX_RESOURCE_URI})
    PRIMARY_OUTPUT(${NOTS_BUILDER_RESOURCE_GLOBAL}/builder)
ENDIF()

END()
